export type Product = {
  slug: string;
  name: string;
  price: number;
  image: string;
  description: string;
};

export type Post = {
  slug: string;
  title: string;
  excerpt: string;
  image: string;
  content: string;
};

